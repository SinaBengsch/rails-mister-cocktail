<div >
  <div class="card-big">
   <img src="/app/assets/images/cocktail-sample.png" alt="cocktail">
   <!-- app/views/articles/index.html.erb -->
    <%#= cl_image_tag("kabmhsgjinxfuyirknct",
        width: 400, height: 300, crop: :fill) %>

    <div class="cocktail-content">
      <h2><%= @cocktail.name %></h2>
      <p>DESCRIPTION</p>
      <%= @cocktail.description %>
    <p>INGREDIENTS</p>
      <% @cocktail.doses.each do |dose| %>
                  <li>
                    <td><%= dose.description %></td>
                    <td><%= dose.ingredient.name %></td>
                    <td>
                      <%= link_to dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } do %>
                        <i class="fa fa-trash destroy_dose"></i>
                      <% end %>
                    </td>
                  </li>
                <% end %>
    <p>INSTRUCTION</p>
      <%= @cocktail.instruction %>
    <p></p>
    </div>

    <div>
      <%= link_to 'Add ingredient', new_cocktail_dose_path(@cocktail), class:"btn-pink" %>
      <%= link_to 'Back to overview', cocktails_path, class:"btn-pink-outline" %>
    </div>
  </div>
</div>
